{
  "name": "take-it",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that syncs Avanza order price to top-of-book for instant-fill limit orders.",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-static-copy": "^2.1.0"
  }
}
